<Application x:Class="HerrNyani.PlaylistFileCopy_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HerrNyani.PlaylistFileCopy_WPF"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ResourceDictionaries/ItemViewModelDataTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="FrameworkElement">
                <Setter Property="Margin" Value="3" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type FrameworkElement}}">
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type FrameworkElement}}">
                <Setter Property="MinHeight" Value="22" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type FrameworkElement}}">
                <Setter Property="MinWidth" Value="75" />
                <Setter Property="Padding" Value="3" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
